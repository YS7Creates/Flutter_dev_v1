import 'package:flutter/material.dart';

class textt extends StatelessWidget {
  const textt({super.key});
  @override
  Widget build(context) {
    return Text(
      'YS7.CREATES',
      style: TextStyle(color: Colors.limeAccent, fontSize: 52.2),
    );
  }
}
